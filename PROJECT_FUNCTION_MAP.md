# 🗺️ Fox Pro AI — Карта Функций Проекта

> Карта основных функций проекта и файлов, которые за них отвечают

---

## 📋 Содержание

1. [Точки входа](#точки-входа)
2. [CLI и команды](#cli-и-команды)
3. [Сканирование проекта](#сканирование-проекта)
4. [Оптимизация](#оптимизация)
5. [Генерация карт](#генерация-карт)
6. [Создание проектов](#создание-проектов)
7. [Утилиты](#утилиты)

---

## 🚪 Точки входа

### `main.py`
**Функция:** Главная точка входа для запуска без установки  
**Что делает:**
- Импортирует `src.cli.main()`
- Запускает CLI интерфейс
- Использование: `python main.py doctor ./project --full`

**Нужен для:** Запуска проекта как скрипта

---

### `src/__main__.py`
**Функция:** Точка входа для запуска как модуль  
**Что делает:**
- Импортирует `src.cli.main()`
- Использование: `python -m src doctor ./project --full`

**Нужен для:** Запуска проекта как Python модуля

---

### `src/cli.py`
**Функция:** Главный CLI интерфейс  
**Что делает:**
- Парсит аргументы командной строки
- Маршрутизирует команды (doctor, create, status, menu)
- Обрабатывает ошибки и показывает help

**Основные функции:**
- `main()` — главная функция CLI
- `create_parser()` — создание парсера аргументов
- `cmd_doctor()` — обработка команды doctor
- `cmd_create()` — обработка команды create
- `cmd_status()` — обработка команды status
- `cmd_menu()` — обработка команды menu

**Нужен для:** Обработки всех команд пользователя

---

## 🩺 CLI и команды

### `src/commands/doctor.py`
**Функция:** Диагностика и оптимизация проектов  
**Что делает:**
- Сканирует проект на проблемы (venv, большие файлы, мусор)
- Показывает отчет о проблемах
- Исправляет проблемы автоматически
- Выполняет полную оптимизацию (перемещение файлов, патчинг, карты)

**Основные функции:**
- `run_doctor()` — главная функция команды doctor
- `diagnose()` — диагностика проекта, поиск проблем
- `fix_issues()` — автоматическое исправление проблем
- `full_optimization()` — полная оптимизация (6 шагов)
- `restore_files()` — восстановление файлов из external storage
- `print_diagnostic_report()` — красивый вывод отчета
- `print_optimization_result()` — красивый вывод результатов оптимизации
- `regenerate_config_paths()` — регенерация config_paths.py

**Нужен для:** Основной функциональности оптимизации проектов

---

### `src/commands/create.py`
**Функция:** Создание новых проектов  
**Что делает:**
- Создает структуру нового проекта
- Генерирует файлы из шаблонов
- Настраивает AI конфиги (.cursorrules, .cursorignore)
- Создает Docker, CI/CD, Git конфиги

**Основные функции:**
- `run_create()` — главная функция создания проекта

**Нужен для:** Создания новых проектов с правильной структурой

---

### `src/commands/status.py`
**Функция:** Показ статуса проекта  
**Что делает:**
- Показывает информацию о проекте
- Проверяет наличие оптимизации
- Показывает статистику файлов

**Основные функции:**
- `run_status()` — главная функция статуса

**Нужен для:** Быстрой проверки состояния проекта

---

### `src/menu.py`
**Функция:** Интерактивное меню  
**Что делает:**
- Показывает интерактивное меню с опциями
- Позволяет выбирать команды через меню
- Показывает справку и примеры

**Основные классы:**
- `FoxMenu` — класс интерактивного меню

**Нужен для:** Удобного интерактивного использования

---

## 🔍 Сканирование проекта

### `src/scanner/token_scanner.py`
**Функция:** Сканирование проекта и подсчет токенов  
**Что делает:**
- Сканирует все файлы проекта
- Подсчитывает токены для каждого файла
- Находит "тяжелые" файлы (>1000 токенов)
- Обнаруживает venv, node_modules, pycache
- Категоризирует файлы (data, code, log, binary, etc.)

**Основные функции:**
- `scan_project()` — главная функция сканирования
- `scan_file()` — сканирование одного файла
- `estimate_tokens()` — оценка токенов в файле
- `categorize_file()` — категоризация файла
- `get_moveable_files()` — получение файлов для перемещения
- `find_all_venvs()` — поиск всех venv директорий
- `is_venv_directory()` — проверка является ли директория venv
- `get_dir_size_mb()` — получение размера директории в MB

**Классы:**
- `FileCategory` — категории файлов (data, code, log, etc.)
- `FileTokens` — информация о токенах файла
- `HeavyFile` — информация о тяжелом файле
- `ScanResult` — результат сканирования проекта

**Нужен для:** Анализа проекта перед оптимизацией

---

## ⚙️ Оптимизация

### `src/optimizer/heavy_mover.py`
**Функция:** Перемещение тяжелых файлов в external storage  
**Что делает:**
- Перемещает большие файлы в external storage
- Создает manifest.json с информацией о перемещенных файлах
- Генерирует config_paths.py (мост к external storage)
- Создает symlinks для динамических путей
- Обновляет .cursorignore
- Восстанавливает файлы обратно

**Основные функции:**
- `move_heavy_files()` — перемещение тяжелых файлов
- `generate_config_paths()` — генерация config_paths.py
- `generate_manifest()` — генерация manifest.json
- `create_symlinks()` — создание symlinks
- `restore_files()` — восстановление файлов
- `update_cursorignore()` — обновление .cursorignore
- `get_external_dir()` — получение пути к external storage
- `get_manifest_path()` — получение пути к manifest.json
- `move_garbage_files()` — перемещение мусорных файлов

**Классы:**
- `MovedFile` — информация о перемещенном файле
- `MoveResult` — результат перемещения
- `GarbageMoveResult` — результат перемещения мусора

**Нужен для:** Перемещения файлов и создания мостов

---

### `src/optimizer/ast_patcher.py`
**Функция:** Патчинг Python кода для использования config_paths  
**Что делает:**
- Находит все использования путей к перемещенным файлам
- Заменяет `open("data/file.json")` на `open(get_path("data/file.json"))`
- Добавляет импорты `from config_paths import get_path`
- Обрабатывает динамические пути (создает symlinks)

**Основные функции:**
- `patch_project()` — патчинг всего проекта
- `patch_file()` — патчинг одного файла
- `PathPatcher` — AST трансформер для замены путей

**Классы:**
- `PatchResult` — результат патчинга
- `PatchReport` — отчет о патчинге
- `PathPatcher` — AST трансформер

**Нужен для:** Автоматического обновления кода после перемещения файлов

---

### `src/optimizer/garbage_cleaner.py`
**Функция:** Очистка мусорных файлов  
**Что делает:**
- Находит мусорные файлы (__pycache__, *.pyc, .DS_Store, etc.)
- Перемещает их в external storage или удаляет

**Нужен для:** Очистки проекта от мусорных файлов

---

## 🗺️ Генерация карт

### `src/mapper/ast_skeleton.py`
**Функция:** Генерация AST скелетов Python файлов  
**Что делает:**
- Парсит Python файлы через AST
- Извлекает структуру (классы, функции, импорты)
- Создает компактные скелеты без тел функций
- Генерирует AST_CODE_MAP.md (компактная и полная версии)

**Основные функции:**
- `generate_ast_code_map()` — главная функция генерации карты
- `generate_project_skeleton()` — генерация скелета проекта
- `extract_skeleton()` — извлечение скелета из файла
- `_extract_function()` — извлечение информации о функции
- `_extract_class()` — извлечение информации о классе
- `skeleton_to_markdown()` — конвертация скелета в Markdown
- `get_string_value()` — извлечение строкового значения (Python 3.7+ совместимость)
- `extract_docstring()` — извлечение docstring
- `safe_unparse()` — безопасный unparse AST (Python 3.7+ совместимость)

**Классы:**
- `FunctionSkeleton` — скелет функции
- `ClassSkeleton` — скелет класса
- `FileSkeleton` — скелет файла
- `ProjectSkeleton` — скелет проекта

**Нужен для:** Создания компактных карт кода для AI

---

### `src/mapper/fox_trace_map.py`
**Функция:** Генерация карты перемещенных файлов  
**Что делает:**
- Создает AST_FOX_TRACE.md с информацией о перемещенных файлах
- Показывает схемы файлов (JSON, CSV, SQLite)
- Показывает использование файлов в коде
- Генерирует компактную и полную версии

**Основные функции:**
- `generate_fox_trace_map()` — генерация карты перемещенных файлов
- `write_fox_trace_md()` — запись AST_FOX_TRACE.md
- `generate_cursor_context()` — генерация контекста для Cursor

**Классы:**
- `TraceFile` — информация о перемещенном файле
- `FoxTraceMap` — карта перемещенных файлов

**Нужен для:** Навигации по перемещенным файлам

---

### `src/mapper/context_map.py`
**Функция:** Генерация контекстной карты проекта  
**Что делает:**
- Создает общую карту структуры проекта
- Показывает связи между модулями

**Нужен для:** Понимания структуры проекта

---

### `src/mapper/schema_extractor.py`
**Функция:** Извлечение схем из файлов  
**Что делает:**
- Извлекает схемы из JSON файлов
- Извлекает схемы из CSV файлов
- Извлекает схемы из SQLite баз данных

**Нужен для:** Понимания структуры данных в перемещенных файлах

---

### `src/mapper/status_generator.py`
**Функция:** Генерация статуса проекта  
**Что делает:**
- Генерирует PROJECT_STATUS.md
- Показывает статус команд, утилит, тестов

**Нужен для:** Документации статуса проекта

---

### `src/mapper/metrics.py`
**Функция:** Метрики и анализ проекта  
**Что делает:**
- Парсит .cursorignore
- Проверяет паттерны игнорирования
- Анализирует метрики проекта

**Нужен для:** Анализа и метрик проекта

---

## 🏗️ Создание проектов

### `src/generators/project_files.py`
**Функция:** Генерация файлов проекта  
**Что делает:**
- Генерирует config.py
- Генерирует README.md
- Генерирует другие файлы проекта

**Нужен для:** Создания структуры нового проекта

---

### `src/generators/ai_configs.py`
**Функция:** Генерация AI конфигов  
**Что делает:**
- Генерирует .cursorrules
- Генерирует .cursorignore
- Генерирует copilot-instructions.md
- Генерирует _AI_INCLUDE/ директорию

**Нужен для:** Настройки AI помощников

---

### `src/generators/docker.py`
**Функция:** Генерация Docker конфигов  
**Что делает:**
- Генерирует Dockerfile
- Генерирует docker-compose.yml
- Генерирует .dockerignore

**Нужен для:** Docker конфигурации

---

### `src/generators/ci_cd.py`
**Функция:** Генерация CI/CD конфигов  
**Что делает:**
- Генерирует GitHub Actions workflows
- Генерирует GitLab CI конфиги

**Нужен для:** CI/CD настройки

---

### `src/generators/git.py`
**Функция:** Генерация Git конфигов  
**Что делает:**
- Генерирует .gitignore
- Инициализирует Git репозиторий

**Нужен для:** Git настройки

---

### `src/generators/scripts.py`
**Функция:** Генерация скриптов  
**Что делает:**
- Генерирует bootstrap.sh
- Генерирует health_check.sh
- Генерирует context_switcher.py

**Нужен для:** Вспомогательных скриптов

---

## 🛠️ Утилиты

### `src/core/paths.py`
**Функция:** Работа с путями и external storage  
**Что делает:**
- Получает пути к external storage
- Работает с manifest.json
- Управляет структурой external storage

**Основные функции:**
- `get_external_root()` — получение корня external storage
- `get_external_data_dir()` — получение директории данных
- `get_external_venvs_dir()` — получение директории venv
- `get_external_garbage_dir()` — получение директории мусора
- `load_manifest()` — загрузка manifest.json
- `save_manifest()` — сохранение manifest.json
- `external_exists()` — проверка существования external storage

**Нужен для:** Управления external storage

---

### `src/core/constants.py`
**Функция:** Константы проекта  
**Что делает:**
- Содержит версию проекта
- Содержит цвета для вывода
- Содержит баннеры и тексты

**Нужен для:** Общих констант проекта

---

### `src/core/config.py`
**Функция:** Конфигурация проекта  
**Что делает:**
- Загружает конфигурацию
- Управляет настройками

**Нужен для:** Конфигурации проекта

---

### `src/core/file_utils.py`
**Функция:** Утилиты для работы с файлами  
**Что делает:**
- Вспомогательные функции для работы с файлами

**Нужен для:** Работы с файлами

---

### `src/core/i18n.py`
**Функция:** Интернационализация  
**Что делает:**
- Управляет переводами
- Загружает локали

**Нужен для:** Многоязычности

---

### `src/types.py`
**Функция:** Типы данных  
**Что делает:**
- Определяет общие типы данных

**Нужен для:** Типизации

---

## 📊 Поток работы оптимизации

```
1. main.py / src/cli.py
   ↓
2. src/commands/doctor.py::run_doctor()
   ↓
3. src/scanner/token_scanner.py::scan_project()
   ↓
4. src/commands/doctor.py::full_optimization()
   ├─ Step 1: Сканирование (token_scanner.py)
   ├─ Step 2: Перемещение файлов (heavy_mover.py)
   ├─ Step 3: Генерация моста (heavy_mover.py::generate_config_paths())
   ├─ Step 4: Патчинг кода (ast_patcher.py)
   ├─ Step 5: Генерация карт (ast_skeleton.py, fox_trace_map.py)
   └─ Step 6: Очистка мусора (garbage_cleaner.py)
```

---

## 🔗 Зависимости между модулями

- `doctor.py` → использует → `token_scanner.py`, `heavy_mover.py`, `ast_patcher.py`, `ast_skeleton.py`, `fox_trace_map.py`
- `heavy_mover.py` → использует → `core/paths.py`
- `ast_patcher.py` → использует → `heavy_mover.py` (для списка перемещенных файлов)
- `ast_skeleton.py` → независимый модуль
- `fox_trace_map.py` → использует → `heavy_mover.py` (для списка перемещенных файлов)

---

## 📝 Примечания

- Все модули используют `core/constants.py` для констант
- Все модули используют `core/paths.py` для работы с путями
- Основная логика находится в `commands/doctor.py`
- Генерация карт находится в `mapper/`
- Оптимизация находится в `optimizer/`

